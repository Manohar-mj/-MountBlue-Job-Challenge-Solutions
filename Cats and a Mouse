def catAndMouse(x, y, z):
    distance_a = abs(z - x)
    distance_b = abs(z - y)
    if distance_a < distance_b:return "Cat A"
    elif distance_a > distance_b:return "Cat B"
    else:return "Mouse C"
