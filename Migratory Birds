def migratoryBirds(arr):
    # Write your code here
    max_type = 0
    max_count = 0
    for i in range(1, 6):
        count_i = arr.count(i)
        if count_i > max_count:
            max_type = i
            max_count = count_i
    return max_type
